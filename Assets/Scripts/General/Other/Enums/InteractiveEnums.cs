using UnityEngine;

namespace InteractiveAndInteractableEnums
{
    public enum levelerType { attackable_rotater,attackable_elevator}
    public enum blockerType { attackble_stoner}
    public enum nailerType { sword_attackeder}


    public enum openerType { trigerable_button, triggerable_debris, triggerable_key }
    public enum switchTpye { alternative_pair, autoresetable_elevator }
    public enum handlerType { moveableplatform_handler }
    public enum resetableType { autoresetable_hider}

    public enum floorType {passable_piper,destroyable_breaker }







    public enum followKeyState { waiting,following,openning}

}
