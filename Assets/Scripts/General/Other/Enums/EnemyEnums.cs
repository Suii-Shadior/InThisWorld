namespace EnemyEnums
{
    public enum HedgehogState { walkstate,deadstate}
    public enum TurtleState { walkstate,fallstate,idlestate}
    public enum ShooterState { idlestate,shootingstate,deadstate}
    public enum SpiderState { hangstate,deadstate}
}
